namespace MiniMaple;

class Program
{
    static void Main(string[] args)
    {
        // var x = new Term("x");
        // var s = x.Pow(2).Mul(3).Add(x.Pow(2)).Add(new Number(1));

        // Console.WriteLine(s);
        // Console.WriteLine(s.Diff());
    }
}
